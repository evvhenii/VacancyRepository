version: "3.3"

services:
  db:
    image: postgres
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=123
      - POSTGRES_DB=cashier
    ports:
      - 5440

  app:
    build: target/
    ports:
      - "8080:8080"
    depends_on:
      - db